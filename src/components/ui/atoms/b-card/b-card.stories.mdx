import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import BCard from "./b-card.vue";

<Meta
  title="Componentes/Atoms/Card"
  component={BCard}
  argTypes={{
    elevation: {
      options: ["100", "200", "300"],
      control: { type: "inline-radio" },
    },
  }}
  decorators={[
    () => ({
      template: '<div style="padding: 16px; display: flex; flex-direction: column;"><story /></div>',
    }),
  ]}
/>

export const Template = (args) => ({
  setup() {
    return { args };
  },
  template: '<BCard v-bind="args">{{ args.default }}</BCard>',
});

## Card

Componente visual para o botão

### Uso normal

<Canvas>
  <Story name="Uso normal" args={{ default: "Default" }}   parameters={{
    layout: 'centered',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Elevações

<Canvas inline={false}>
  <Story
    name="Elevação/100"
    args={{ default: "Elevation 100", elevation: "100" }}
      parameters={{
    layout: 'centered',
  }}
  >
    {Template.bind({})}
  </Story>
  <Story name="Elevação/200" args={{ default: "Elevation 200", elevation: "200" }}   parameters={{
    layout: 'centered',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Elevação/300" args={{ default: "Elevation 300", elevation: "300" }}   parameters={{
    layout: 'centered',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={BCard} />
