import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import BText from "./b-text.vue";

<Meta
  title="Componentes/Atoms/Text"
  component={BText}
  argTypes={{
    elevation: {
      options: ["100", "200", "300"],
      control: { type: "inline-radio" },
    },
  }}
/>

export const Template = (args) => ({
  setup() {
    return { args };
  },
  template: '<BText v-bind="args">{{ args.default }}</BText>',
});

## Card

Componente visual para o botão

### Uso normal

<Canvas>
  <Story
    name="Uso normal"
    args={{ default: "Default" }}
    parameters={{
      layout: "centered",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Variantes

<Canvas inline={false}>
  <Story
    name="Variante/H1"
    args={{ default: "h1", variant: "h1" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/H2"
    args={{ default: "h2", variant: "h2" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/H3"
    args={{ default: "h3", variant: "h3" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/H4"
    args={{ default: "h4", variant: "h4" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/H5"
    args={{ default: "h5", variant: "h5" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/H6"
    args={{ default: "h6", variant: "h6" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/Subtitle 1"
    args={{ default: "subtitle1", variant: "subtitle1" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/Subtitle 2"
    args={{ default: "subtitle2", variant: "subtitle2" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/Body 1"
    args={{ default: "body1", variant: "body1" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/Body 2"
    args={{ default: "body2", variant: "body2" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/Button"
    args={{ default: "button", variant: "button" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/Caption"
    args={{ default: "caption", variant: "caption" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Variante/Overline"
    args={{ default: "overline", variant: "overline" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Tamanhos

Os tamanhos seguem a escala progressiva de 10-40px.

<Canvas inline={false}>
  <Story
    name="Tamanho/Inherit"
    args={{ default: "inherit", size: "inherit" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/nano"
    args={{ default: "nano", size: "nano" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/micro"
    args={{ default: "micro", size: "micro" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/extra-small"
    args={{ default: "extra-small", size: "extra-small" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/small"
    args={{ default: "small", size: "small" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/medium"
    args={{ default: "medium", size: "medium" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/large"
    args={{ default: "large", size: "large" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/extra-large"
    args={{ default: "extra-large", size: "extra-large" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/jumbo"
    args={{ default: "jumbo", size: "jumbo" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/extra-jumbo"
    args={{ default: "extra-jumbo", size: "extra-jumbo" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/giant"
    args={{ default: "giant", size: "giant" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/extra-giant"
    args={{ default: "extra-giant", size: "extra-giant" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/colossal"
    args={{ default: "colossal", size: "colossal" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/extra-colossal"
    args={{ default: "extra-colossal", size: "extra-colossal" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/mega"
    args={{ default: "mega", size: "mega" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/extra-mega"
    args={{ default: "extra-mega", size: "extra-mega" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tamanho/immense"
    args={{ default: "immense", size: "immense" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Pesos

<Canvas inline={false}>
  <Story
    name="Peso/Inherit"
    args={{ default: "inherit", weight: "inherit" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/thin"
    args={{ default: "thin", weight: "thin" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/extra-light"
    args={{ default: "extra-light", weight: "extra-light" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/light"
    args={{ default: "light", weight: "light" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/regular"
    args={{ default: "regular", weight: "regular" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/medium"
    args={{ default: "medium", weight: "medium" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/semi-bold"
    args={{ default: "semi-bold", weight: "semi-bold" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/bold"
    args={{ default: "bold", weight: "bold" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/extra-bold"
    args={{ default: "extra-bold", weight: "extra-bold" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Peso/black"
    args={{ default: "black", weight: "black" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Ênfase

<Canvas inline={false}>
  <Story
    name="Ênfase/Inherit"
    args={{ default: "inherit", emphasis: "inherit" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Ênfase/low"
    args={{ default: "low", emphasis: "low" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Ênfase/medium"
    args={{ default: "medium", emphasis: "medium" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Ênfase/high"
    args={{ default: "high", emphasis: "high" }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Comportamento

<Canvas inline={false}>
  <Story
    name="Comportamentos/italic"
    args={{ default: "italic", italic: true }}
    parameters={{
      layout: "padded",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Alinhamento

<Canvas inline={false}>
  <Story
    name="Alinhamento/Inherit"
    args={{ default: "inherit", align: "inherit" }}
    parameters={{
      layout: "padded",
    }}
    decorators={[
      () => ({
        template: '<div style="min-width: 6em; background-color: grey;"><story /></div>',
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Alinhamento/left"
    args={{ default: "left", align: "left" }}
    parameters={{
      layout: "padded",
    }}
    decorators={[
      () => ({
        template: '<div style="min-width: 6em; background-color: grey;"><story /></div>',
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Alinhamento/right"
    args={{ default: "right", align: "right" }}
    parameters={{
      layout: "padded",
    }}
    decorators={[
      () => ({
        template: '<div style="min-width: 6em; background-color: grey;"><story /></div>',
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Alinhamento/center"
    args={{ default: "center", align: "center" }}
    parameters={{
      layout: "padded",
    }}
    decorators={[
      () => ({
        template: '<div style="min-width: 6em; background-color: grey;"><story /></div>',
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Alinhamento/justify"
    args={{ default: "justify", align: "justify" }}
    parameters={{
      layout: "padded",
    }}
    decorators={[
      () => ({
        template: '<div style="min-width: 6em; background-color: grey;"><story /></div>',
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Cores

<Canvas inline={false}>
  <Story
    name="Cores/inherit"
    args={{ default: "inherit", color: 'inherit' }}
    parameters={{
      layout: "padded",
    }}
    decorators={[
      () => ({
        template: '<div style="min-width: 6em; background-color: grey;"><story /></div>',
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Comportamentos/inverse"
    args={{ default: "inverse", color: 'inverse' }}
    parameters={{
      layout: "padded",
    }}
    decorators={[
      () => ({
        template: '<div style="background-color: grey;"><story /></div>',
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={BText} />
